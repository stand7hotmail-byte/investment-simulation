[build]
builder = "NIXPACKS"

[deploy]
startCommand = "alembic upgrade head && python -m app.seed_assets && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/assets"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
