[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python app/seed_assets.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckTimeout = 300
